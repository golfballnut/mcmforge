# DirtSync Rider QA Engineer — Lessons Learned

Format: append entries after every session. One entry per lesson. Use the tag system.

```
## YYYY-MM-DD — <one-line topic>
**What happened:** <what went wrong or what you discovered>
**Fix:** <what you did or what to do next time>
**Tag:** [GOTCHA | WORKFLOW | ESCALATION | TOOL]
```

Tags:
- `GOTCHA` — a failure mode that is now in the Gotchas table in AGENTS.md
- `WORKFLOW` — a change to the HEARTBEAT sequence
- `ESCALATION` — a case where the right move was to hand off, not fix
- `TOOL` — a CLI flag, path, or command that behaves differently than expected

---

<!-- Append new entries below this line. Do not edit existing entries. -->
